{
  "name": "tiles",
  "version": "1.0.0",
  "description": "Using browserify and the ol package",
  "main": "tiles.js",
  "scripts": {
    "build": "browserify -g [ babelify --presets [ \"@babel/preset-env\" ] ] -g uglifyify --entry tiles.js > bundle.js"
  },
  "dependencies": {
    "http-server": "^0.12.3",
    "ol": "^5.1.3"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0-beta.56",
    "@babel/preset-env": "^7.0.0-beta.56",
    "babelify": "^9.0.0",
    "browserify": "^16.2.2",
    "uglifyify": "^5.0.1"
  }
}
